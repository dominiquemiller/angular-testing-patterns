import { FancyBoxDirectiveDirective } from './fancy-box.directive';

describe('FancyBoxDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FancyBoxDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
